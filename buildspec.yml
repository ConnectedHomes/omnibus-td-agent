version: 0.2

env:
  variables:
    LANG: "C.UTF-8"
    TD_BUILD_VERSION: "6.10.0"
    TD_BUILD_ITERATION: "1"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  build:
    commands:
      - env
      - docker build . --no-cache
      - docker tag td-agent:$TD_BUILD_VERSION-$TD_BUILD_ITERATION
      # - command
    finally:
      # - command
  post_build:
    commands:
      # - command
    #finally:
      # - command
      # - command
#artifacts:
  #files:
    # - location
    # - location
    # - name
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - path
    # - path
